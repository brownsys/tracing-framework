This is a version of X-Trace implemented on top of the generic Brown tracing framework.

The client library contains methods for creating and configuring X-Trace loggers, starting X-Trace tasks, and logging X-Trace events.

The details of the X-Trace report format are located in the xtrace-common package
The details of metadata propagation and system instrumentation are located in the tracing package